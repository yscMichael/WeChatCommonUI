<!-- 刷新动画 -->
<view class='title-cell'
      bindtap='clickRefresh'
      data-jump='refresh'>刷新动画</view>

<!-- 常见布局方式 -->
<view class='title-cell'
      bindtap='clickRefresh'
      data-jump='commonLayout'>常见布局方式</view>

<!-- 左右布局UI -->
<view class='title-cell'
      bindtap='clickRefresh'
      data-jump='leftAndRightLayout'>左右布局</view>

<!-- 左右布局UI -->
<view class='title-cell'
      bindtap='clickRefresh'
      data-jump='collectionLayout'>collectionView布局</view>

<!-- 跟随列表 -->
<view class='title-cell'
      bindtap='clickRefresh'
      data-jump='followList'>跟随列表</view>

<!-- 左侧弹出框 -->
<view class='title-cell'
      bindtap='clickRefresh'
      data-jump='leftPop'>左侧弹出框</view>

<!-- 实验整个模版覆盖 -->
<view class='title-cell'
      bindtap='clickRefresh'
      data-jump='leftPop'>实验整个模版覆盖</view>

<!-- absolute实验 -->
<view class='title-cell'
      bindtap='clickRefresh'
      data-jump='absoluteTest'>absolute实验</view>

<!-- scrollView刷新 -->
<view class='title-cell'
      bindtap='clickRefresh'
      data-jump='refreshScrollView'>scrollView刷新</view>

